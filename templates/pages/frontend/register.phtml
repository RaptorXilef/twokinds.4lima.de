<?php

declare(strict_types=1);
$baseUrl = \rtrim($settings['base_url'] ?? '', '/');
include __DIR__ . '/../../layouts/frontend/_public_header.phtml'; ?>
<main class="content-area">
    <article class="auth-container">
        <header>
            <h1 class="page-header">Konto erstellen</h1>
            <p>Speichere deine Lesezeichen sicher in der Cloud.</p>
        </header>
        <div class="content-box-shadow">
            <div id="reg-status-msg"
                 class="msg-box"></div>
            <form id="frontend-reg-form"
                  class="admin-form">
                <!-- HONEYPOT FÜR BOTS (Unsichtbar für Menschen) -->
                <div class="honeypot-field">
                    <label>Middle Name</label>
                    <input type="text"
                           name="middle_name"
                           tabindex="-1"
                           autocomplete="off">
                </div>
                <div class="form-group mb-15">
                    <label>Benutzername</label>
                    <input type="text"
                           name="username"
                           required
                           minlength="3"
                           placeholder="Sichtbar für andere">
                </div>
                <div class="form-group mb-15">
                    <label>E-Mail Adresse</label>
                    <input type="email"
                           name="email"
                           required
                           placeholder="Für Login und Benachrichtigungen">
                </div>
                <div class="form-group mb-20">
                    <label>Passwort (min. 8 Zeichen)</label>
                    <input type="password"
                           name="password"
                           required
                           minlength="8">
                </div>
                <div class="form-group mb-20">
                    <label>Passwort wiederholen</label>
                    <input type="password"
                           name="password_confirm"
                           required
                           minlength="8">
                </div>
                <button type="submit"
                        id="reg-submit-btn"
                        class="button add">Registrieren</button>
            </form>
            <div class="form-links">
                <span>Bereits ein Konto? <a href="<?php echo $baseUrl; ?>/login" class="text-link">Hier einloggen</a></span>
            </div>
        </div>
    </article>
</main>
<?php include __DIR__ . '/../../layouts/frontend/_public_footer.phtml'; ?>
