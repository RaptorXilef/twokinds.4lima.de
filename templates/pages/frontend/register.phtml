<?php

declare(strict_types=1);
$baseUrl = \rtrim($settings['base_url'] ?? '', '/');
include __DIR__ . '/../../components/frontend/_public_header.phtml';
?>
<main class="content-area">
    <article style="width: 100%; max-width: 500px; margin: 0 auto; padding: 20px;">
        <header style="text-align: center; margin-bottom: 30px;">
            <h1 class="page-header"
                style="border: none; margin-bottom: 10px;">Konto erstellen</h1>
            <p style="color: var(--text-color-faded);">Speichere deine Lesezeichen sicher in der Cloud.</p>
        </header>

        <div
             style="background: var(--content-box-bg); padding: 30px; border-radius: 8px; border: 1px solid var(--border-medium); box-shadow: 0 4px 6px rgba(0,0,0,0.1);">
            <div id="reg-status-msg"
                 style="display: none; padding: 10px; border-radius: 4px; margin-bottom: 20px; font-weight: bold; text-align: center;">
            </div>

            <form id="frontend-reg-form"
                  class="admin-form">
                <!-- HONEYPOT FÜR BOTS (Unsichtbar für Menschen) -->
                <div style="display: none;">
                    <label>Middle Name</label>
                    <input type="text"
                           name="middle_name"
                           tabindex="-1"
                           autocomplete="off">
                </div>

                <div class="form-group"
                     style="margin-bottom: 15px;">
                    <label style="display: block; font-weight: bold; margin-bottom: 5px;">Benutzername</label>
                    <input type="text"
                           name="username"
                           required
                           minlength="3"
                           placeholder="Sichtbar für andere"
                           style="width: 100%; padding: 10px; border: 1px solid var(--border-medium); border-radius: 4px; background: var(--content-bg); color: var(--text-color);">
                </div>
                <div class="form-group"
                     style="margin-bottom: 15px;">
                    <label style="display: block; font-weight: bold; margin-bottom: 5px;">E-Mail Adresse</label>
                    <input type="email"
                           name="email"
                           required
                           placeholder="Für Login und Benachrichtigungen"
                           style="width: 100%; padding: 10px; border: 1px solid var(--border-medium); border-radius: 4px; background: var(--content-bg); color: var(--text-color);">
                </div>
                <div class="form-group"
                     style="margin-bottom: 20px;">
                    <label style="display: block; font-weight: bold; margin-bottom: 5px;">Passwort (min. 8
                        Zeichen)</label>
                    <input type="password"
                           name="password"
                           required
                           minlength="8"
                           style="width: 100%; padding: 10px; border: 1px solid var(--border-medium); border-radius: 4px; background: var(--content-bg); color: var(--text-color);">
                </div>
                <button type="submit"
                        id="reg-submit-btn"
                        class="button add"
                        style="width: 100%; padding: 12px; font-size: 1.1em; font-weight: bold;">Registrieren</button>
            </form>

            <div style="text-align: center; margin-top: 20px; font-size: 0.9em;">
                Bereits ein Konto? <a href="<?php echo $baseUrl; ?>/login" style="font-weight: bold;">Hier einloggen</a>
            </div>
        </div>
    </article>
</main>

<script nonce="<?php echo \htmlspecialchars($_SESSION['csrf_token'] ?? ''); ?>">
document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('frontend-reg-form');
    const msg = document.getElementById('reg-status-msg');
    const btn = document.getElementById('reg-submit-btn');

    if (form) {
        form.addEventListener('submit', async (e) => {
            e.preventDefault();
            btn.disabled = true;
            btn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Erstelle Konto...';

            const formData = new FormData(form);
            formData.append('csrf_token', '<?php echo \htmlspecialchars($_SESSION['csrf_token'] ?? ''); ?>');

            try {
                const res = await fetch('<?php echo $baseUrl; ?>/api/frontend_register', {
                    method: 'POST',
                    body: formData
                });
                const json = await res.json();

                msg.style.display = 'block';
                if (json.success) {
                    msg.style.backgroundColor = 'var(--status-green-bg)';
                    msg.style.color = 'var(--status-green-text)';
                    msg.style.border = '1px solid var(--status-green-border)';
                    msg.innerHTML = '<i class="fa-solid fa-check"></i> ' + json.message;
                    setTimeout(() => window.location.href = '<?php echo $baseUrl; ?>/' + json.redirect, 1000);
                } else {
                    msg.style.backgroundColor = 'var(--status-red-bg)';
                    msg.style.color = 'var(--status-red-text)';
                    msg.style.border = '1px solid var(--status-red-border)';
                    msg.innerHTML = '<i class="fa-solid fa-triangle-exclamation"></i> ' + json
                        .error;
                    btn.disabled = false;
                    btn.textContent = 'Registrieren';
                }
            } catch (err) {
                msg.style.display = 'block';
                msg.style.backgroundColor = 'var(--status-red-bg)';
                msg.style.color = 'var(--status-red-text)';
                msg.innerHTML = 'Verbindungsfehler.';
                btn.disabled = false;
                btn.textContent = 'Registrieren';
            }
        });
    }
});

</script>
<?php include __DIR__ . '/../../components/frontend/_public_footer.phtml'; ?>
