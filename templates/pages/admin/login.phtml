<?php

declare(strict_types=1);

use App\Contracts\System\AssetHelperInterface;

/**
 * @var array $settings
 * @var string $pageTitle
 * @var AssetHelperInterface $asset
 */
$baseUrl = \rtrim($settings['base_url'], '/');
$bodyClass = 'page-login';

include __DIR__ . '/../../layouts/admin/_admin_header.phtml';
?>

<aside class="l-sidebar">
    <div class="l-sidebar__inner">
        <nav id="menu"
             class="menu"
             style="margin-top: 100px;">
            <a href="<?php echo $baseUrl; ?>/">Zurück zur Webseite</a>
            <a id="toggle_lights" class="theme" href="#" title="Design umschalten"></a>
            <!-- JS lädt hier sofort das richtige Icon rein -->
        </nav>
    </div>
</aside>

<div class="l-content-box">
    <div class="admin-login-container">
        <h2>Systemzugang</h2>
        <p class="intro-text">Bitte melde dich an, um auf die Verwaltungsoberfläche zuzugreifen.</p>

        <div id="login-status-message"
             class="status-message"></div>

        <form id="admin-login-form"
              class="admin-form"
              method="POST">
            <div class="form-group">
                <label for="username">Benutzername</label>
                <input type="text"
                       id="username"
                       name="username"
                       required
                       autocomplete="username"
                       autofocus>
            </div>

            <div class="form-group">
                <label for="password">Passwort</label>
                <div class="password-wrapper">
                    <input type="password"
                           id="password"
                           name="password"
                           required
                           autocomplete="current-password">
                </div>
            </div>

            <button type="submit"
                    id="login-submit-btn">Anmelden</button>
        </form>
    </div>

    <?php include __DIR__ . '/../../layouts/admin/_admin_footer.phtml'; ?>

    <!-- Das NEUE, modulare Script laden -->
    <script type="module"
            nonce="<?php echo \htmlspecialchars($_SESSION['csrf_token'] ?? ''); ?>"
            src="<?php echo $asset->url('assets/js/admin/login.js'); ?>"></script>
    </body>

    </html>
