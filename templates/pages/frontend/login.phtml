<?php

declare(strict_types=1);
$baseUrl = \rtrim($settings['base_url'] ?? '', '/');
include __DIR__ . '/../../layouts/frontend/_public_header.phtml';
?>
<main class="content-area">
    <article class="auth-container">
        <header>
            <h1 class="page-header">Willkommen zurück!</h1>
            <p>Logge dich ein, um deine Lesezeichen zu synchronisieren.</p>
        </header>

        <!-- Flash-Messages (z.B. für Bestätigungslink-Erfolg) -->
        <?php if (! empty($flashes['success'])) { ?>
            <div class="status-message status-green visible mb-20">
                <i class="fa-solid fa-check"></i> <?php echo \htmlspecialchars($flashes['success'][0]); ?>
            </div>
        <?php } ?>
        <?php if (! empty($flashes['error'])) { ?>
            <div class="status-message status-red visible mb-20">
                <i class="fa-solid fa-triangle-exclamation"></i> <?php echo \htmlspecialchars($flashes['error'][0]); ?>
            </div>
        <?php } ?>
        <?php if (! empty($flashes['info'])) { ?>
            <div class="status-message status-info visible mb-20">
                <i class="fa-solid fa-circle-info"></i> <?php echo \htmlspecialchars($flashes['info'][0]); ?>
            </div>
        <?php } ?>
        <div class="content-box-shadow">
            <div id="login-status-msg"
                 class="msg-box"></div>
            <form id="frontend-login-form"
                  class="admin-form">
                <div class="form-group mb-15">
                    <label>Benutzername oder E-Mail</label>
                    <input type="text"
                           name="username"
                           required>
                </div>
                <div class="form-group mb-20">
                    <label>Passwort</label>
                    <input type="password"
                           name="password"
                           required>
                </div>
                <button type="submit"
                        id="login-submit-btn"
                        class="button edit">Einloggen</button>
            </form>
            <div class="form-links">
                <div class="form-links-row">
                    <a href="<?php echo $baseUrl; ?>/passwort-vergessen">Passwort vergessen?</a>
                    <a href="<?php echo $baseUrl; ?>/bestaetigungsmail-anfordern">Aktivierungs-Mail fehlt?</a>
                </div>
                <span class="mt-10">Noch kein Konto? <a href="<?php echo $baseUrl; ?>/registrieren" class="text-link">Jetzt registrieren</a></span>
            </div>
        </div>
    </article>
</main>
<?php include __DIR__ . '/../../layouts/frontend/_public_footer.phtml'; ?>
