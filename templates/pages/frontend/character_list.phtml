<?php

declare(strict_types=1);

/**
 * @var array $characters
 * @var array $groups
 * @var string $pageTitle
 * @var array $settings
 * @var array $filterData
 */
$baseUrl = \rtrim($settings['base_url'], '/');
include __DIR__ . '/../../layouts/frontend/_public_header.phtml'; ?>
<main>
    <article class="max-w-900 w-100 center-block">
        <header>
            <h1 class="page-header">Charakter Übersicht</h1>
        </header>
        <p class="character-overview-intro">Eine alphabetische Übersicht aller Charaktere, eingeteilt in Gruppen.</p>

        <!-- START FILTER BEREICH -->
        <div class="filter-form content-box-shadow mb-30">
            <h3 class="section-title"><i class="fa-solid fa-filter text-link"></i> Charaktere filtern </h3>
            <div class="filter-controls grid-auto-180">
                <div style="grid-column: 1 / -1;">
                    <label class="d-block font-small font-bold mb-5 text-faded">Freitext-Suche</label>
                    <input type="text"
                           id="char-filter-search"
                           placeholder="Name, Alias oder Biographie durchsuchen...">
                </div>
                <div>
                    <label class="d-block font-small font-bold mb-5 text-faded">Geschlecht</label>
                    <select id="char-filter-gender">
                        <option value="">Alle</option>
                        <?php foreach ($filterData['gender'] as $v) {
                            echo '<option value="' . \htmlspecialchars((string) $v) . '">' . \htmlspecialchars((string) $v) . '</option>';
                        } ?>
                    </select>
                </div>
                <div>
                    <label class="d-block font-small font-bold mb-5 text-faded">Alter</label>
                    <select id="char-filter-age">
                        <option value="">Alle</option>
                        <?php foreach ($filterData['age'] as $v) {
                            echo '<option value="' . \htmlspecialchars((string) $v) . '">' . \htmlspecialchars((string) $v) . '</option>';
                        } ?>
                    </select>
                </div>
                <div>
                    <label class="d-block font-small font-bold mb-5 text-faded">Spezies</label>
                    <select id="char-filter-species">
                        <option value="">Alle</option>
                        <?php foreach ($filterData['species'] as $v) {
                            echo '<option value="' . \htmlspecialchars((string) $v) . '">' . \htmlspecialchars((string) $v) . '</option>';
                        } ?>
                    </select>
                </div>
                <div>
                    <label class="d-block font-small font-bold mb-5 text-faded">Subspezies</label>
                    <select id="char-filter-subspecies">
                        <option value="">Alle</option>
                        <?php foreach ($filterData['subspecies'] as $v) {
                            echo '<option value="' . \htmlspecialchars((string) $v) . '">' . \htmlspecialchars((string) $v) . '</option>';
                        } ?>
                    </select>
                </div>
                <div>
                    <label class="d-block font-small font-bold mb-5 text-faded">Klasse/Rang</label>
                    <select id="char-filter-rank">
                        <option value="">Alle</option>
                        <?php foreach ($filterData['rank'] as $v) {
                            echo '<option value="' . \htmlspecialchars((string) $v) . '">' . \htmlspecialchars((string) $v) . '</option>';
                        } ?>
                    </select>
                </div>
                <div>
                    <label class="d-block font-small font-bold mb-5 text-faded">Sprachen</label>
                    <select id="char-filter-languages">
                        <option value="">Alle</option>
                        <?php foreach ($filterData['languages'] as $v) {
                            echo '<option value="' . \htmlspecialchars((string) $v) . '">' . \htmlspecialchars((string) $v) . '</option>';
                        } ?>
                    </select>
                </div>
                <div>
                    <label class="d-block font-small font-bold mb-5 text-faded">Haarfarbe</label>
                    <select id="char-filter-haircolor">
                        <option value="">Alle</option>
                        <?php foreach ($filterData['hairColor'] as $v) {
                            echo '<option value="' . \htmlspecialchars((string) $v) . '">' . \htmlspecialchars((string) $v) . '</option>';
                        } ?>
                    </select>
                </div>
                <div>
                    <label class="d-block font-small font-bold mb-5 text-faded">Augenfarbe</label>
                    <select id="char-filter-eyecolor">
                        <option value="">Alle</option>
                        <?php foreach ($filterData['eyeColor'] as $v) {
                            echo '<option value="' . \htmlspecialchars((string) $v) . '">' . \htmlspecialchars((string) $v) . '</option>';
                        } ?>
                    </select>
                </div>
                <div>
                    <label class="d-block font-small font-bold mb-5 text-faded">Fellfarbe</label>
                    <select id="char-filter-furcolor">
                        <option value="">Alle</option>
                        <?php foreach ($filterData['furColor'] as $v) {
                            echo '<option value="' . \htmlspecialchars((string) $v) . '">' . \htmlspecialchars((string) $v) . '</option>';
                        } ?>
                    </select>
                </div>
            </div>
            <div class="text-right mt-15">
                <button type="button"
                        id="char-filter-reset"
                        class="button edit p-10 font-small"><i class="fa-solid fa-rotate-left"></i> Filter
                    zurücksetzen</button>
            </div>
        </div>
        <!-- ENDE FILTER BEREICH -->

        <?php $limitToIds = null;
include __DIR__ . '/../../partials/frontend/_character_display.phtml'; ?>
    </article>
</main>
<?php include __DIR__ . '/../../layouts/frontend/_public_footer.phtml'; ?>
