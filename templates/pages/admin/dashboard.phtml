<?php
declare(strict_types=1);

/**
 * @var array                                      $settings
 * @var string                                     $pageTitle
 * @var string                                     $adminUser
 * @var \App\Core\Entity\ComicPage[]               $comics
 * @var \App\Core\Entity\Character[]               $characters
 * @var \App\Core\Entity\Report[]                  $openReports
 * @var \App\Contracts\System\AssetHelperInterface $asset
 */
$baseUrl = \rtrim($settings['base_url'], '/');
?>
<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?php echo \htmlspecialchars($pageTitle); ?> - Twokinds Admin</title>
    <link rel="stylesheet" href="<?php echo $asset->url('assets/css/main.min.css'); ?>">
    <!-- FontAwesome (falls du es nutzt) für Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <meta name="csrf-token" content="<?php echo \htmlspecialchars($_SESSION['csrf_token'] ?? ''); ?>">
    <style>
        /* Ein kleines bisschen Layout-Voodoo für die Menü-Tabs */
        .content-section { display: none; }
        .content-section.active { display: block; }
    </style>
</head>
<body class="<?php echo isset($_COOKIE['themePref']) && $_COOKIE['themePref'] === '2' ? 'theme-night' : ''; ?>">

<div class="main-container">
    <header>
        <div class="banner" id="banner-lights-off"></div>
    </header>

    <div class="content-area">
        <aside class="sidebar">
            <div class="sidebar-content">
                <div class="session-timer">
                    <i class="fa-solid fa-user-shield"></i> <strong><?php echo \htmlspecialchars($adminUser); ?></strong>
                </div>

                <nav id="menu" class="menu">
                    <span class="menu-label">Verwaltung</span>
                    <a href="#section-comics" class="tab-link active" data-target="section-comics">Comics</a>
                    <a href="#section-characters" class="tab-link" data-target="section-characters">Charaktere</a>

                    <span class="menu-label">System</span>
                    <a href="#section-reports" class="tab-link" data-target="section-reports">
                        Reports
                        <?php if (\count($openReports) > 0) { ?>
                            <span style="background: #dc3545; color: white; border-radius: 10px; padding: 2px 6px; font-size: 0.8em; margin-left: 5px;">
                                <?php echo \count($openReports); ?>
                            </span>
                        <?php } ?>
                    </a>

                    <span class="menu-label">Account</span>
                    <a href="<?php echo $baseUrl; ?>/" target="_blank">Zur Webseite <i class="fa-solid fa-external-link-alt" style="font-size: 0.8em;"></i></a>
                    <a href="#" id="admin-logout-btn" class="logout-link"><i class="fa-solid fa-sign-out-alt"></i> Abmelden</a>

                    <a id="toggle_lights" class="theme" href="#">
                        <span class="themelabel">Theme</span>
                        <span class="themename">LICHT AUS</span>
                    </a>
                </nav>
            </div>
        </aside>

        <div class="content">
            <h1 class="page-header"><i class="fa-solid fa-gauge"></i> <?php echo \htmlspecialchars($pageTitle); ?></h1>

            <div id="global-status-message" class="status-message"></div>

            <!-- ========================================== -->
            <!-- SECTION: COMICS                            -->
            <!-- ========================================== -->
            <section id="section-comics" class="content-section active">
                <div class="table-controls actions-bar">
                    <div class="top-actions">
                        <button type="button" class="button add" id="btn-add-comic">
                            <i class="fa-solid fa-plus"></i> Neuen Comic hinzufügen
                        </button>
                    </div>
                </div>

                <div class="sitemap-table-container">
                    <table class="admin-table comic-editor-table">
                        <thead>
                            <tr>
                                <th>ID / Datum</th>
                                <th>Thumbnail</th>
                                <th>Typ & Kapitel</th>
                                <th>Titel & Transkript</th>
                                <th>Aktionen</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php if (empty($comics)) { ?>
                                <tr>
                                    <td colspan="5" class="empty-table-message">Keine Comics gefunden.</td>
                                </tr>
                            <?php } else { ?>
                                <?php foreach ($comics as $comic) {
                                    $cId       = $comic->id->value;
                                    $cb        = $comic->imageUpdatedAt ? '?c=' . $comic->imageUpdatedAt : '';
                                    $thumbPath = "{$baseUrl}/assets/images/comic/thumbnails/{$cId}.webp{$cb}";

                                    // Alle Daten für JS bereitstellen
                                    $charIds   = \array_map(fn ($id) => $id->value, $comic->characterIds);
                                    $comicData = \json_encode([
                                        'id'          => $cId,
                                        'type'        => $comic->type,
                                        'name'        => $comic->name,
                                        'chapterId'   => $comic->chapterId ?? '',
                                        'originalUrl' => $comic->originalUrl,
                                        'sketchUrl'   => $comic->sketchUrl,
                                        'transcript'  => $comic->transcript ?? '',
                                        'characters'  => $charIds,
                                    ], \JSON_HEX_APOS | \JSON_HEX_QUOT);
                                    ?>
                                    <tr>
                                        <td class="mono"><?php echo \htmlspecialchars($cId); ?></td>
                                        <td>
                                            <img src="<?php echo \htmlspecialchars($thumbPath); ?>" class="comic-thumbnail" loading="lazy">
                                        </td>
                                        <td>
                                            <span class="comic-type-display"><?php echo \htmlspecialchars($comic->type); ?></span>
                                            <?php if ($comic->chapterId) { ?>
                                                <small>Kapitel <?php echo \htmlspecialchars($comic->chapterId); ?></small>
                                            <?php } ?>
                                        </td>
                                        <td>
                                            <strong><?php echo \htmlspecialchars($comic->name !== '' ? $comic->name : 'Kein Titel'); ?></strong>
                                            <?php if ($comic->transcript) { ?>
                                                <div class="description-preview"><?php echo \strip_tags($comic->transcript); ?></div>
                                            <?php } ?>
                                        </td>
                                        <td class="actions-cell">
                                            <form>
                                                <button type="button" class="button button-orange btn-undo-comic" data-id="<?php echo \htmlspecialchars($cId); ?>" title="Letzte Änderung rückgängig machen">
                                                    <i class="fa-solid fa-rotate-left"></i>
                                                </button>
                                                <button type="button" class="button edit btn-edit-comic" data-payload='<?php echo $comicData; ?>' title="Bearbeiten">
                                                    <i class="fa-solid fa-pen"></i>
                                                </button>
                                                <button type="button" class="button delete btn-delete-comic" data-id="<?php echo \htmlspecialchars($cId); ?>" title="Löschen">
                                                    <i class="fa-solid fa-trash"></i>
                                                </button>
                                            </form>
                                        </td>
                                    </tr>
                                <?php } ?>
                            <?php } ?>
                        </tbody>
                    </table>
                </div>
            </section>

            <!-- ========================================== -->
            <!-- SECTION: CHARAKTERE                        -->
            <!-- ========================================== -->
            <section id="section-characters" class="content-section">
                <div class="table-controls actions-bar">
                    <div class="top-actions">
                        <button type="button" class="button add" id="btn-add-char">
                            <i class="fa-solid fa-plus"></i> Neuen Charakter erstellen
                        </button>
                    </div>
                </div>

                <div class="sitemap-table-container">
                    <table class="admin-table">
                        <thead>
                            <tr>
                                <th style="width: 100px;">ID</th>
                                <th style="width: 80px;">Avatar</th>
                                <th>Name & Beschreibung</th>
                                <th style="width: 120px; text-align: right;">Aktionen</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php if (empty($characters)) { ?>
                                <tr>
                                    <td colspan="4" class="empty-table-message">Keine Charaktere angelegt.</td>
                                </tr>
                            <?php } else { ?>
                                <?php foreach ($characters as $char) {
                                    $cId = $char->id->value;
                                    $pic = $char->picUrl ? "{$baseUrl}/assets/images/characters/profiles/{$char->picUrl}" : 'https://placehold.co/100x100?text=N/A';

                                    $charData = \json_encode([
                                        'id'          => $cId,
                                        'name'        => $char->name,
                                        'picUrl'      => $char->picUrl ?? '',
                                        'description' => $char->description ?? '',
                                    ], \JSON_HEX_APOS | \JSON_HEX_QUOT);
                                    ?>
                                    <tr>
                                        <td class="mono"><?php echo \htmlspecialchars($cId); ?></td>
                                        <td>
                                            <img src="<?php echo \htmlspecialchars($pic); ?>" style="width: 50px; height: 50px; object-fit: cover; border-radius: 50%;">
                                        </td>
                                        <td>
                                            <strong><?php echo \htmlspecialchars($char->name); ?></strong>
                                            <?php if ($char->description) { ?>
                                                <div class="description-preview" style="max-height: 2.5em;"><?php echo \strip_tags($char->description); ?></div>
                                            <?php } ?>
                                        </td>
                                        <td class="actions-cell">
                                            <form>
                                                <button type="button" class="button edit btn-edit-char" data-payload='<?php echo $charData; ?>' title="Bearbeiten">
                                                    <i class="fa-solid fa-pen"></i>
                                                </button>
                                                <button type="button" class="button delete btn-delete-char" data-id="<?php echo \htmlspecialchars($cId); ?>" data-name="<?php echo \htmlspecialchars($char->name); ?>" title="Löschen">
                                                    <i class="fa-solid fa-trash"></i>
                                                </button>
                                            </form>
                                        </td>
                                    </tr>
                                <?php } ?>
                            <?php } ?>
                        </tbody>
                    </table>
                </div>
            </section>

            <!-- ========================================== -->
            <!-- SECTION: REPORTS                           -->
            <!-- ========================================== -->
            <section id="section-reports" class="content-section">
                <p>Das Report-System wird in Kürze implementiert.</p>
            </section>

        </div>
    </div>
</div>

<!-- ========================================== -->
<!-- MODALS                                     -->
<!-- ========================================== -->

<!-- Comic Modal -->
<div id="comic-modal" class="modal">
    <div class="modal-overlay btn-close-comic-modal"></div>
    <div class="modal-content" style="max-width: 800px;">
        <div class="modal-header-wrapper">
            <h2 id="modal-title-comic">Comic bearbeiten</h2>
            <button class="modal-close btn-close-comic-modal">&times;</button>
        </div>
        <div class="modal-scroll-content">
            <form id="comic-form" class="admin-form">
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                    <div class="form-group">
                        <label for="comic_id">Comic ID (YYYYMMDD)</label>
                        <input type="text" id="comic_id" name="comic_id" required pattern="\d{8}" placeholder="z.B. 20251024">
                    </div>
                    <div class="form-group">
                        <label for="type">Typ</label>
                        <select id="type" name="type">
                            <option value="Comicseite">Comicseite</option>
                            <option value="Filler">Filler</option>
                            <option value="Art">Art</option>
                        </select>
                    </div>
                </div>

                <div style="display: grid; grid-template-columns: 2fr 1fr; gap: 15px;">
                    <div class="form-group">
                        <label for="name">Titel / Name</label>
                        <input type="text" id="name" name="name" placeholder="Name des Comics">
                    </div>
                    <div class="form-group">
                        <label for="chapter_id">Kapitel ID</label>
                        <input type="text" id="chapter_id" name="chapter_id" placeholder="z.B. 21">
                    </div>
                </div>

                <!-- Keenspot Base URL Logik ist im Backend verbaut! -->
                <div class="form-group">
                    <label for="url_originalbild">Original URL (Bild)</label>
                    <input type="text" id="url_originalbild" name="url_originalbild" placeholder="Dateiname (z.B. 12345.jpg) oder volle https:// URL">
                </div>

                <div class="form-group">
                    <label for="url_originalsketch">Original URL (Sketch)</label>
                    <input type="text" id="url_originalsketch" name="url_originalsketch" placeholder="Dateiname oder volle https:// URL">
                </div>

                <div class="form-group">
                    <label for="transcript">Transkript (HTML)</label>
                    <textarea id="transcript" name="transcript" class="summernote-code-look" rows="5"></textarea>
                </div>

                <!-- Hinweis: Charaktere kommen in der nächsten Ausbaustufe! -->
            </form>
        </div>
        <div class="modal-footer-actions">
            <div class="modal-buttons">
                <button type="button" class="button btn-close-comic-modal">Abbrechen</button>
                <button type="button" class="button edit" id="btn-save-comic">Speichern (Live)</button>
            </div>
        </div>
    </div>
</div>

<!-- Character Modal -->
<div id="char-modal" class="modal">
    <div class="modal-overlay btn-close-char-modal"></div>
    <div class="modal-content" style="max-width: 600px;">
        <div class="modal-header-wrapper">
            <h2 id="modal-title-char">Charakter bearbeiten</h2>
            <button class="modal-close btn-close-char-modal">&times;</button>
        </div>
        <div class="modal-scroll-content">
            <form id="char-form" class="admin-form">
                <input type="hidden" id="character_id" name="character_id" value="new">

                <div class="form-group">
                    <label for="char_name">Name des Charakters</label>
                    <input type="text" id="char_name" name="name" required placeholder="z.B. Trace Legacy">
                </div>

                <div class="form-group">
                    <label for="pic_url">Dateiname des Profilbildes</label>
                    <input type="text" id="pic_url" name="pic_url" placeholder="z.B. trace.jpg">
                </div>

                <div class="form-group">
                    <label for="char_description">Beschreibung (HTML)</label>
                    <textarea id="char_description" name="description" class="summernote-code-look" rows="5"></textarea>
                </div>
            </form>
        </div>
        <div class="modal-footer-actions">
            <div class="modal-buttons">
                <button type="button" class="button btn-close-char-modal">Abbrechen</button>
                <button type="button" class="button edit" id="btn-save-char">Speichern (Live)</button>
            </div>
        </div>
    </div>
</div>

<script nonce="<?php echo \htmlspecialchars($_SESSION['csrf_token'] ?? ''); ?>" src="<?php echo $asset->url('assets/js/common.js'); ?>"></script>
<script nonce="<?php echo \htmlspecialchars($_SESSION['csrf_token'] ?? ''); ?>" src="<?php echo $asset->url('assets/js/admin.js'); ?>"></script>
</body>
</html>
