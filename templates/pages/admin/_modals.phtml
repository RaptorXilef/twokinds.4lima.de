<?php

declare(strict_types=1);

use App\Core\Entity\Character;
use App\Core\Entity\CharacterGroup;

/**
 * @var string           $baseUrl
 * @var Character[]      $characters
 * @var CharacterGroup[] $groups
 * @var string[]         $existingRanks
 * @var string[]         $existingChapters
 * @var string[]         $availableImages
 */
?>

<style>
/* Trumbowyg Button Reset: Schützt den Editor vor globalen Button-Styles */
.trumbowyg-button-pane button {
    padding: 0 !important;
    margin: 0 !important;
    min-width: 0 !important;
    min-height: 0 !important;
    background: transparent !important;
    border: none !important;
    box-shadow: none !important;
    line-height: normal !important;
}

.trumbowyg-box {
    margin-top: 0 !important;
}

/* CSS für den Bild-Klick-Zoom (Lightbox) */
#image-hover-overlay {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: rgba(0, 0, 0, 0.85);
    z-index: 9999;
    justify-content: center;
    align-items: center;
    cursor: zoom-out;
    /* NEU: Cursor zeigt an, dass man es wegklicken kann */
}

#image-hover-overlay img {
    max-width: 90vw;
    max-height: 95vh;
    border-radius: 8px;
    box-shadow: 0 0 30px rgba(0, 0, 0, 0.8);
    border: 2px solid var(--border-medium);
}

.preview-box {
    background: var(--table-row-even);
    border: 1px solid var(--border-medium);
    border-radius: 6px;
    padding: 10px;
    text-align: center;
}

.hover-zoom-trigger {
    cursor: zoom-in;
    border-radius: 4px;
    border: 1px solid var(--border-medium);
    margin-top: 10px;
    max-height: 140px;
    object-fit: contain;
    background: #fff;
}

</style>

<!-- ========================================== -->
<!-- MODALS                                     -->
<!-- ========================================== -->

<!-- Comic Modal -->
<div id="comic-modal"
     class="modal">
    <div class="modal-overlay btn-close-comic-modal"></div>
    <div class="modal-content"
         style="max-width: 1080px; display: flex; flex-direction: column; max-height: 95vh;">
        <div class="modal-header-wrapper"
             style="flex: 0 0 auto;">
            <h2 id="modal-title-comic">Comic bearbeiten</h2>
            <button class="modal-close btn-close-comic-modal">&times;</button>
        </div>
        <div class="modal-scroll-content"
             style="flex: 1 1 auto; overflow-y: auto;">

            <!-- NEU: Die 3 Live-Vorschauen -->
            <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 15px; margin-bottom: 20px;">
                <div class="preview-box">
                    <strong>Lokal (Thumbnail)</strong><br>
                    <img id="prev-comic-local"
                         src="https://placehold.co/100x140?text=Kein+Bild"
                         class="hover-zoom-trigger">
                </div>
                <div class="preview-box">
                    <strong>Original (Keenspot)</strong><br>
                    <img id="prev-comic-orig"
                         src="https://placehold.co/100x140?text=Kein+Bild"
                         class="hover-zoom-trigger">
                </div>
                <div class="preview-box">
                    <strong>Sketch (Keenspot)</strong><br>
                    <img id="prev-comic-sketch"
                         src="https://placehold.co/100x140?text=Kein+Bild"
                         class="hover-zoom-trigger">
                </div>
            </div>

            <form id="comic-form"
                  class="admin-form">
                <input type="hidden"
                       id="form-action"
                       name="action"
                       value="save">

                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin-bottom: 15px;">
                    <div class="form-group">
                        <label for="comic_id">Comic ID (YYYYMMDD) *</label>
                        <input type="text"
                               id="comic_id"
                               name="comic_id"
                               required
                               pattern="\d{8}"
                               placeholder="z.B. 20251024">
                        <small style="color: var(--text-color-faded);">
                            Füllt URLs beim Verlassen des Feldes automatisch aus.
                        </small>
                    </div>
                    <div class="form-group">
                        <label for="type">Typ</label>
                        <select id="type"
                                name="type">
                            <option value="Comicseite">Comicseite</option>
                            <option value="Filler">Filler</option>
                            <option value="Art">Art</option>
                        </select>
                    </div>
                </div>

                <div style="display: grid; grid-template-columns: 2fr 1fr; gap: 15px; margin-bottom: 15px;">
                    <div class="form-group">
                        <label for="name">Titel / Name</label>
                        <input type="text"
                               id="name"
                               name="name"
                               placeholder="Name des Comics">
                    </div>
                    <div class="form-group">
                        <label for="chapter_id">Kapitel ID</label>
                        <input type="text"
                               id="chapter_id"
                               name="chapter_id"
                               list="chapter-datalist"
                               placeholder="z.B. 21">
                        <datalist id="chapter-datalist">
                            <?php foreach ($existingChapters ?? [] as $ch) { ?>
                                <option value="<?php echo \htmlspecialchars((string) $ch); ?>">
                                <?php } ?>
                        </datalist>
                    </div>
                </div>

                <!-- Keenspot Base URL Logik ist im Backend verbaut! -->
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin-bottom: 15px;">
                    <div class="form-group">
                        <label for="url_originalbild">Original URL (Bild)</label>
                        <input type="text"
                               id="url_originalbild"
                               name="url_originalbild"
                               placeholder="Dateiname (z.B. 12345.jpg) oder volle https:// URL">
                    </div>

                    <div class="form-group">
                        <label for="url_originalsketch">Original URL (Sketch)</label>
                        <input type="text"
                               id="url_originalsketch"
                               name="url_originalsketch"
                               placeholder="Dateiname oder volle https:// URL">
                    </div>
                </div>

                <div class="form-group">
                    <label for="transcript">Transkript (HTML)</label>
                    <textarea id="transcript" name="transcript" class="wysiwyg-editor" rows="5"></textarea>
                </div>

                <div class="form-group"
                     style="margin-top: 15px;">
                    <div style="display: flex; justify-content: space-between; align-items: center;">
                        <label>Auf der Seite vorkommende Charaktere</label>
                        <button type="button"
                                class="button"
                                id="btn-toggle-char-view"><i class="fa-solid fa-layer-group"></i> Gruppiert
                            anzeigen</button>
                    </div>

                    <!-- Das versteckte Select-Feld sammelt die echten Daten für den Request -->
                    <select name="character_ids[]"
                            id="hidden-comic-chars"
                            multiple
                            style="display:none;">
                        <?php foreach ($characters as $char) { ?>
                            <option value="<?php echo \htmlspecialchars($char->id->value); ?>"></option>
                        <?php } ?>
                    </select>

                    <div
                         style="background: var(--table-row-even); border: 1px solid var(--border-medium); border-radius: 4px; padding: 10px;">

                        <!-- Ansicht 1: Alphabetisch -->
                        <div id="view-chars-alpha"
                             class="char-selection-grid"
                             style="max-height: 250px; overflow-y: auto;">
                            <?php
                            $alphaChars = $characters;
\usort($alphaChars, fn ($a, $b) => \strcasecmp($a->name, $b->name));
foreach ($alphaChars as $char) {
    $pic = $char->picUrl ? "{$baseUrl}/assets/images/characters/profiles/{$char->picUrl}" : 'https://placehold.co/60x60?text=N/A';
    ?>
                                <div class="char-selection-item"
                                     data-char-id="<?php echo \htmlspecialchars($char->id->value); ?>">
                                    <img src="<?php echo \htmlspecialchars($pic); ?>"
                                         loading="lazy">
                                    <span><?php echo \htmlspecialchars($char->name); ?></span>
                                </div>
                            <?php } ?>
                        </div>

                        <!-- Ansicht 2: Gruppiert -->
                        <div id="view-chars-grouped"
                             style="display: none; max-height: 250px; overflow-y: auto;">
                            <?php foreach ($groups as $group) { ?>
                                <h4 style="margin: 10px 0 5px 0; border-bottom: 1px solid var(--border-medium);">
                                    <?php echo \htmlspecialchars($group->name); ?></h4>
                                <div class="char-selection-grid"
                                     style="margin-bottom: 15px;">
                                    <?php foreach ($group->characterIds as $cidObj) {
                                        $char = null;
                                        foreach ($characters as $c) {
                                            if ($c->id->value === $cidObj->value) {
                                                $char = $c;

                                                break;
                                            }
                                        }
                                        if (! $char) {
                                            continue;
                                        }
                                        $pic = $char->picUrl ? "{$baseUrl}/assets/images/characters/profiles/{$char->picUrl}" : 'https://placehold.co/60x60?text=N/A';
                                        ?>
                                        <div class="char-selection-item"
                                             data-char-id="<?php echo \htmlspecialchars($char->id->value); ?>">
                                            <img src="<?php echo \htmlspecialchars($pic); ?>"
                                                 loading="lazy">
                                            <span><?php echo \htmlspecialchars($char->name); ?></span>
                                        </div>
                                    <?php } ?>
                                </div>
                            <?php } ?>
                        </div>

                    </div>
                </div>
            </form>

        </div>
        <div class="modal-footer-actions"
             style="flex: 0 0 auto; position: relative; z-index: 10; background: var(--main-bg);">
            <div class="modal-buttons">
                <button type="button"
                        class="button btn-close-comic-modal">Abbrechen</button>
                <button type="button"
                        class="button edit"
                        id="btn-save-comic">Speichern (Live)</button>
            </div>
        </div>
    </div>
</div>

<!-- Charakter Modal -->
<div id="char-modal"
     class="modal">
    <div class="modal-overlay btn-close-char-modal"></div>
    <div class="modal-content"
         style="max-width: 1080px; display: flex; flex-direction: column; max-height: 95vh;">
        <div class="modal-header-wrapper"
             style="flex: 0 0 auto;">
            <h2 id="modal-title-char">Charakter bearbeiten</h2>
            <button class="modal-close btn-close-char-modal">&times;</button>
        </div>
        <div class="modal-scroll-content"
             style="flex: 1 1 auto; overflow-y: auto;">

            <div style="display: flex; gap: 20px; align-items: flex-start; margin-bottom: 15px;">
                <!-- Linke Spalte: Vorschaubild -->
                <div style="flex: 0 0 120px; text-align: center;">
                    <img id="char-preview-img"
                         src="https://placehold.co/120x120?text=Kein+Bild"
                         style="width: 120px; height: 120px; object-fit: cover; border-radius: 50%; border: 3px solid var(--border-medium); background: #fff;">
                    <div style="margin-top: 10px; font-weight: bold; color: var(--text-color-faded);"
                         id="char-display-id">ID: NEW</div>
                </div>

                <!-- Rechte Spalte: Formular -->
                <div style="flex: 1;">
                    <form id="char-form"
                          class="admin-form"
                          enctype="multipart/form-data">
                        <input type="hidden"
                               id="character_id"
                               name="character_id"
                               value="new">

                        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin-bottom: 15px;">
                            <div class="form-group">
                                <label for="char_name">Name *</label>
                                <input type="text"
                                       id="char_name"
                                       name="name"
                                       required
                                       placeholder="z.B. Trace Legacy">
                            </div>
                            <div class="form-group">
                                <label for="alt_names">Alternative Namen</label>
                                <input type="text"
                                       id="alt_names"
                                       name="alt_names"
                                       placeholder="Z.B. Großtempler">
                            </div>
                        </div>

                        <div class="form-group"
                             style="margin-bottom: 15px;">
                            <label for="char_rank">Rang (Optional)</label>
                            <input type="text"
                                   id="char_rank"
                                   name="rank"
                                   list="rank-datalist"
                                   placeholder="Tippen oder auswählen...">
                            <datalist id="rank-datalist">
                                <?php foreach ($existingRanks as $rank) { ?>
                                    <option value="<?php echo \htmlspecialchars($rank); ?>">
                                    <?php } ?>
                            </datalist>
                        </div>

                        <div class="form-group"
                             style="margin-bottom: 15px;">
                            <label>Profilbild Upload & Auswahl</label>
                            <!-- UX FIX: Buttons und Input untereinander, damit es nicht quetscht -->
                            <div style="display: flex; flex-direction: column; gap: 10px; margin-bottom: 10px;">
                                <button type="button"
                                        class="button"
                                        id="btn-open-gallery"
                                        style="width: 100%;"><i class="fa-solid fa-images"></i> Aus Galerie
                                    wählen</button>
                                <input type="text"
                                       id="pic_url"
                                       name="pic_url"
                                       placeholder="Oder Dateiname manuell eintippen (z.B. trace.webp)"
                                       style="width: 100%;">
                            </div>

                            <!-- Drag & Drop Zone für neuen Upload -->
                            <div id="char-drop-zone"
                                 class="drag-drop-zone"
                                 style="padding: 15px; border: 2px dashed var(--border-medium); border-radius: 6px; text-align: center; cursor: pointer; background: var(--table-row-even);">
                                <i class="fa-solid fa-cloud-arrow-up"
                                   style="font-size: 2em; color: var(--text-color-light);"></i>
                                <p style="margin: 5px 0 0; color: var(--text-color-light);">Neues Bild hier ablegen oder
                                    klicken</p>
                                <input type="file"
                                       id="profile_image"
                                       name="profile_image"
                                       accept="image/*"
                                       style="display: none;">
                                <div id="upload-preview-name"
                                     style="margin-top: 5px; font-weight: bold; color: var(--link-color);"></div>
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="char_description">Beschreibung (HTML)</label>
                            <textarea id="char_description"
                                      name="description"
                                      class="wysiwyg-editor"
                                      rows="4">
                            </textarea>
                        </div>
                    </form>
                </div>
            </div>

        </div>
        <div class="modal-footer-actions"
             style="flex: 0 0 auto; position: relative; z-index: 10; background: var(--main-bg);">
            <div class="modal-buttons">
                <button type="button"
                        class="button btn-close-char-modal">Abbrechen</button>
                <button type="button"
                        class="button edit"
                        id="btn-save-char">Speichern (Live)</button>
            </div>
        </div>
    </div>
</div>

<!-- Image Gallery Modal -->
<div id="gallery-modal"
     class="modal"
     style="z-index: 1050;">
    <div class="modal-overlay btn-close-gallery-modal"></div>
    <div class="modal-content"
         style="max-width: 800px;">
        <div class="modal-header-wrapper">
            <h2>Bild aus Galerie wählen</h2>
            <button class="modal-close btn-close-gallery-modal">&times;</button>
        </div>
        <div class="modal-scroll-content">
            <div class="char-selection-grid">
                <?php foreach ($availableImages as $img) {
                    $url = "{$baseUrl}/assets/images/characters/profiles/{$img}";
                    ?>
                    <div class="char-selection-item gallery-item"
                         data-filename="<?php echo \htmlspecialchars($img); ?>"
                         data-url="<?php echo \htmlspecialchars($url); ?>">
                        <img src="<?php echo \htmlspecialchars($url); ?>"
                             loading="lazy"
                             style="width:60px;height:60px;object-fit:cover;border-radius:5px;">
                        <span style="font-size: 0.75em; word-break: break-all;"><?php echo \htmlspecialchars($img); ?></span>
                    </div>
                <?php } ?>
            </div>
        </div>
    </div>
</div>

<!-- Das globale Overlay für das Hover-Zoom der Comic-Vorschauen -->
<div id="image-hover-overlay">
    <img src=""
         id="hover-overlay-img"
         alt="Zoom Preview">
</div>

<!-- Report Detail Modal -->
<div id="report-detail-modal"
     class="modal">
    <div class="modal-overlay btn-close-report-modal"></div>
    <div class="modal-content"
         style="max-width: 900px; display: flex; flex-direction: column; max-height: 95vh;">
        <div class="modal-header-wrapper"
             style="flex: 0 0 auto;">
            <h2>Meldung überprüfen</h2>
            <button class="modal-close btn-close-report-modal">&times;</button>
        </div>
        <div class="modal-scroll-content"
             style="flex: 1 1 auto; overflow-y: auto;">

            <div class="report-info-grid"
                 style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 10px; background: var(--table-row-even); padding: 15px; border-radius: 5px; margin-bottom: 20px;">
                <div><strong>Comic ID:</strong> <span id="rep-modal-comic-id" class="mono"></span></div>
                <div><strong>Melder:</strong> <span id="rep-modal-submitter"></span></div>
                <div><strong>Datum:</strong> <span id="rep-modal-date"></span></div>
            </div>

            <div class="form-group"
                 style="margin-bottom: 20px;">
                <label>Beschreibung des Nutzers:</label>
                <div id="rep-modal-desc"
                     style="padding: 15px; background: var(--content-area-bg); border: 1px solid var(--border-medium); border-radius: 4px; white-space: pre-wrap;">
                </div>
            </div>

            <div id="rep-modal-transcript-section"
                 style="display: none; margin-bottom: 20px;">
                <label>Transkript-Abgleich (Diff):</label>
                <div class="diff-box"
                     id="rep-modal-diff"
                     style="padding: 15px; background: #1e1e1e; color: #d4d4d4; border-radius: 4px; max-height: 300px; overflow-y: auto; font-size: 14px;">
                </div>

                <div style="margin-top: 10px; text-align: right;">
                    <button type="button"
                            class="button"
                            id="btn-transfer-transcript"
                            style="background: #28a745; color: white;">
                        <i class="fa-solid fa-file-import"></i> Im Comic-Editor öffnen & einfügen
                    </button>
                </div>
            </div>

            <details style="margin-top: 20px;">
                <summary style="cursor: pointer; color: var(--text-color-light);">Telemetrie & Debug-Infos (für
                    Administratoren)</summary>
                <textarea id="rep-modal-debug" rows="6" class="summernote-code-look" readonly style="margin-top: 10px; width: 100%;"></textarea>
            </details>

        </div>
        <div class="modal-footer-actions"
             style="flex: 0 0 auto; display: flex; justify-content: space-between; z-index: 10; background: var(--main-bg);">
            <div>
                <button type="button"
                        class="button delete"
                        id="btn-rep-spam"
                        style="background: #dc3545;"><i class="fa-solid fa-trash"></i> Als Spam markieren</button>
                <button type="button"
                        class="button edit"
                        id="btn-rep-resolve"
                        style="background: #28a745;"><i class="fa-solid fa-check"></i> Als Erledigt markieren</button>
            </div>
            <button type="button"
                    class="button btn-close-report-modal">Schließen</button>
        </div>
    </div>
</div>
