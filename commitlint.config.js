/**
 * Beschreibung: Konfiguration für Commitlint (Conventional Commits).
 * @file commitlint.config.js
 * @since 5.0.0
 */

module.exports = {
    extends: ['@commitlint/config-conventional'],
    rules: {
        // Hier könntest du eigene Regeln definieren,
        // der Standard reicht aber für Perfektionisten völlig aus. xD
    }
};
