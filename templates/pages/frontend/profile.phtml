<?php

declare(strict_types=1);
/**
 * @var array                 $settings
 * @var string                $pageTitle
 * @var \App\Core\Entity\User $user
 */
$baseUrl = \rtrim($settings['base_url'] ?? '', '/');
include __DIR__ . '/../../components/frontend/_public_header.phtml';
?>
<main class="content-area">
    <article style="width: 100%; max-width: 600px; margin: 0 auto; padding: 20px;">
        <header style="text-align: center; margin-bottom: 30px;">
            <h1 class="page-header"
                style="border: none; margin-bottom: 10px;">
                <i class="fa-solid fa-user-gear"
                   style="color: var(--link-color);"></i> Profil verwalten
            </h1>
            <p style="color: var(--text-color-faded);">E-Mail: <?php echo \htmlspecialchars($user->email); ?></p>
        </header>

        <div
             style="background: var(--content-box-bg); padding: 30px; border-radius: 8px; border: 1px solid var(--border-medium); box-shadow: 0 4px 6px rgba(0,0,0,0.1); margin-bottom: 20px;">
            <h3 style="margin-top:0; border-bottom: 1px solid var(--border-light); padding-bottom: 10px;">Benutzername
            </h3>
            <div class="msg-box"
                 id="msg-username"
                 style="display:none; padding:10px; margin-bottom:15px; border-radius:4px; font-weight:bold; text-align:center;">
            </div>

            <form id="form-username"
                  class="admin-form">
                <input type="hidden"
                       name="update_type"
                       value="username">
                <div class="form-group"
                     style="margin-bottom: 15px;">
                    <input type="text"
                           name="new_username"
                           required
                           minlength="3"
                           value="<?php echo \htmlspecialchars($user->username); ?>"
                           style="width: 100%; padding: 10px; border: 1px solid var(--border-medium); border-radius: 4px; background: var(--content-bg); color: var(--text-color);">
                </div>
                <button type="submit"
                        class="button edit"
                        style="width:100%; padding: 10px; font-weight:bold;">Namen ändern</button>
            </form>
        </div>

        <div
             style="background: var(--content-box-bg); padding: 30px; border-radius: 8px; border: 1px solid var(--border-medium); box-shadow: 0 4px 6px rgba(0,0,0,0.1); margin-bottom: 20px;">
            <h3 style="margin-top:0; border-bottom: 1px solid var(--border-light); padding-bottom: 10px;">Passwort
                ändern</h3>
            <div class="msg-box"
                 id="msg-password"
                 style="display:none; padding:10px; margin-bottom:15px; border-radius:4px; font-weight:bold; text-align:center;">
            </div>

            <form id="form-password"
                  class="admin-form">
                <input type="hidden"
                       name="update_type"
                       value="password">
                <div class="form-group"
                     style="margin-bottom: 15px;">
                    <input type="password"
                           name="old_password"
                           required
                           placeholder="Altes Passwort"
                           style="width: 100%; padding: 10px; border: 1px solid var(--border-medium); border-radius: 4px; background: var(--content-bg); color: var(--text-color);">
                </div>
                <div class="form-group"
                     style="margin-bottom: 15px;">
                    <input type="password"
                           name="new_password"
                           required
                           minlength="8"
                           placeholder="Neues Passwort (min. 8 Zeichen)"
                           style="width: 100%; padding: 10px; border: 1px solid var(--border-medium); border-radius: 4px; background: var(--content-bg); color: var(--text-color);">
                </div>
                <button type="submit"
                        class="button edit"
                        style="width:100%; padding: 10px; font-weight:bold;">Passwort speichern</button>
            </form>
        </div>

        <div
             style="background: var(--content-box-bg); padding: 30px; border-radius: 8px; border: 1px solid var(--border-medium); box-shadow: 0 4px 6px rgba(0,0,0,0.1);">
            <h3 style="margin-top:0; border-bottom: 1px solid var(--border-light); padding-bottom: 10px;">Newsletter &
                Updates</h3>
            <div class="msg-box"
                 id="msg-newsletter"
                 style="display:none; padding:10px; margin-bottom:15px; border-radius:4px; font-weight:bold; text-align:center;">
            </div>

            <form id="form-newsletter">
                <input type="hidden"
                       name="update_type"
                       value="newsletter">
                <div style="margin-bottom: 20px;">
                    <label style="display: flex; align-items: center; gap: 10px; cursor: pointer;">
                        <input type="checkbox"
                               name="wants_newsletter"
                               value="1"
                               style="transform: scale(1.5);"
                               <?php echo $user->wantsNewsletter ? 'checked' : ''; ?>>
                        <span>Ich möchte eine E-Mail bekommen, sobald eine neue Comic-Seite fertig übersetzt wurde!</span>
                    </label>
                </div>
                <button type="submit"
                        class="button add"
                        style="width:100%; padding: 10px; font-weight:bold;"><i class="fa-solid fa-save"></i>
                    Einstellungen speichern</button>
            </form>
        </div>

    </article>
</main>

<script nonce="<?php echo \htmlspecialchars($_SESSION['csrf_token'] ?? ''); ?>">
document.addEventListener('DOMContentLoaded', () => {
    const handleForm = (formId, msgId) => {
        const form = document.getElementById(formId);
        const msg = document.getElementById(msgId);
        if (!form) return;

        form.addEventListener('submit', async (e) => {
            e.preventDefault();
            const btn = form.querySelector('button[type="submit"]');
            const origText = btn.innerHTML;
            btn.disabled = true;
            btn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Lade...';

            const formData = new window.FormData(form);
            formData.append('csrf_token', '<?php echo \htmlspecialchars($_SESSION['csrf_token'] ?? ''); ?>');

            try {
                const res = await fetch('<?php echo $baseUrl; ?>/api/frontend_update_profile', {
                    method: 'POST',
                    body: formData
                });
                const json = await res.json();

                msg.style.display = 'block';
                if (json.success) {
                    msg.style.backgroundColor = 'var(--status-green-bg)';
                    msg.style.color = 'var(--status-green-text)';
                    msg.style.border = '1px solid var(--status-green-border)';
                    msg.innerHTML = '<i class="fa-solid fa-check"></i> ' + json.message;
                    if (formId === 'form-password') form.reset();
                    if (formId === 'form-username') {
                        setTimeout(() => window.location.reload(),
                        1500); // Reload for new name in header
                    }
                } else {
                    msg.style.backgroundColor = 'var(--status-red-bg)';
                    msg.style.color = 'var(--status-red-text)';
                    msg.style.border = '1px solid var(--status-red-border)';
                    msg.innerHTML = '<i class="fa-solid fa-triangle-exclamation"></i> ' + json
                        .error;
                }
            } catch (err) {
                msg.style.display = 'block';
                msg.style.backgroundColor = 'var(--status-red-bg)';
                msg.style.color = 'var(--status-red-text)';
                msg.innerHTML = 'Verbindungsfehler.';
            }
            btn.disabled = false;
            btn.innerHTML = origText;
        });
    };

    handleForm('form-username', 'msg-username');
    handleForm('form-password', 'msg-password');
    handleForm('form-newsletter', 'msg-newsletter');
});

</script>
<?php include __DIR__ . '/../../components/frontend/_public_footer.phtml'; ?>
